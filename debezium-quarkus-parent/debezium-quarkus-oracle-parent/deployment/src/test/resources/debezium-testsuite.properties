debezium.offset.storage=org.apache.kafka.connect.storage.MemoryOffsetBackingStore
debezium.schema.history.internal=io.debezium.relational.history.MemorySchemaHistory
debezium.name=test
debezium.topic.prefix=topic
debezium.topic.naming.strategy=io.quarkus.debezium.testsuite.deployment.naming.LowercaseSchemaTopicNamingStrategy
debezium.database.names=inventory
debezium.database.dbname=FREE
debezium.database.pdb.name=FREEPDB1
debezium.snapshot.mode=initial
debezium.schema.include.list=INVENTORY
debezium.capturing.orders.destination=topic.inventory.orders
debezium.include.schema.changes=false
debezium.heartbeat.interval.ms=5

quarkus.datasource.db-kind=oracle
quarkus.datasource.username=${ORACLE_USERNAME}
quarkus.datasource.password=${ORACLE_PASSWORD}

quarkus.datasource.jdbc.url=${ORACLE_JDBC}
quarkus.datasource.jdbc.max-size=16

